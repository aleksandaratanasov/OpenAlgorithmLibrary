#ifndef HEAPSORT_H
#define HEAPSORT_H

#include <cstdlib>
#include <array>

using std::array;

namespace sorting {
  namespace heapsort {
    template <typename T, size_t S>
    void sort(std::array<T,S> &a) {
    }
  }
}

#endif // HEAPSORT_H
