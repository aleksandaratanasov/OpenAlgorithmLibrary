#include "testgenerators.h"

/*
namespace testing {
  void generateIntTest(void *target, size_t size, ttype testType, long int randRangeMin, long int randRangeMax) {
//    if(target == nullptr)
//      target = new array<int

    std::vector<int> *tmp = new std::vector<int>(size);
    target = tmp->;
  }
}*/
