#ifndef MERGESORT_H
#define MERGESORT_H

#include <cstdlib>
#include <array>

using std::array;

namespace sorting {
  namespace mergesort {
    template <typename T, size_t S>
    void sort(std::array<T,S> &a) {
    }
  }
}

#endif // MERGESORT_H
